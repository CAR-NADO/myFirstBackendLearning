# There is two types of importing in js. "commonjs" and "module"
commonjs -> require("express"),  module -> import Express from "express"

# Dev dependency - means a library/package will use only on development not in production stage
# Multer is use for file uploading configer.
# cookieParser is use for to "Access" and "Set" cookies from server to user browser










# Video Number 8 - Inprogress (Custom api response and error handling | chai aur backend) || 20:00